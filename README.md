# asteroids
basic "low level" programmed asteroids game in C++ using raylib

# to-do
 - [DONE] add score system
 - [DONE] add lives system
 - [DONE] fix generateRandomVelocity() method (only generates positive values)
 - add ship blinking while in invulnerable state
 - refactor some game.cpp functions and move them to aux_functions.cpp (e.g. collision-checkers)
 - enhance gameover screen (draw background objects gray to give depth effect)
 - add some sort of config menu to manually change game features (e.g. wave_multiplier, asteroid_size)
